# Fix for peer dependency conflicts
legacy-peer-deps=true

# Ensure consistent package installation
save-exact=true

# Faster installation and better disk usage
shamefully-hoist=true

# Security: audit before install
audit=true
